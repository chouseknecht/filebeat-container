# Add your Ansible Container service definitions here.
# For example:
  
filebeat:
  image: "centos:7" 
  user: filebeat
  command: ['filebeat', '-e', '-c', '/etc/filebeat']

  # volumes:
  # - <volume with filebeat config>:/etc/filebeat
   
